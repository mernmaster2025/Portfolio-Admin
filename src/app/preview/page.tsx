import PreviewFrame from "@/components/preview/PreviewFrame";

// Standalone preview document, embedded by the builder via an iframe.
export default function PreviewPage() {
  return <PreviewFrame />;
}
